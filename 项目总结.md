# Persistent Persona QQ Bot 项目总结

## 项目定位

AIQQBot 是一个基于 NoneBot2 与 OneBot V11 的 QQ 单角色机器人。项目当前角色名为“雪”，重点不是一次性的问答，而是让角色在长期运行中保持稳定的人格、世界观、关系记忆和连续生活经历。

项目通过 NapCat 接入 QQ，支持私聊与可配置的群聊回复，并可连接 DeepSeek、Claude、LM Studio 或其他 OpenAI 兼容模型。

## 已实现能力

- **QQ 消息接入**：通过 OneBot V11 反向 WebSocket 接收和发送消息。
- **私聊与群聊**：群聊可完全关闭、仅响应 `@机器人`，或响应所有普通群消息。
- **统一模型层**：主聊天和后台任务共用统一客户端，后台模型可以独立配置，也可以回退到主模型。
- **稳定角色设定**：人格、边界、说话方式和确定发生过的经历保存在 `config/persona.json`。
- **世界观约束**：法律、地理、家庭和社会背景集中保存在 `config/world.json`，避免多处设定互相冲突。
- **关系与长期记忆**：使用 SQLite 保存消息、摘要、情节记忆、心理图式、情绪状态和事件候选。
- **心理事件评价**：模型提出事件候选，程序验证证据、去重并评分；当前默认使用安全的影子模式，不直接改写正式人格与记忆。
- **自主生活系统**：角色可在真实时间中经历吃饭、休息、求职、家庭联系和照顾流浪猫等事件；模型选择行为，程序负责校验可行性并结算后果。
- **提醒与主动消息**：支持提醒、离开后的跟进和每日生活日程。
- **Windows 一键启动**：自动检查 Python、创建 `.venv`、安装依赖，并同时启动机器人和相邻目录中的 NapCat。

## 技术结构

| 目录/文件 | 职责 |
| --- | --- |
| `bot.py` | NoneBot 启动入口与 OneBot V11 适配器注册 |
| `plugins/ai_chat/` | 私聊、群聊、回复和意图处理入口 |
| `plugins/scheduler.py` | 提醒、跟进和唤醒任务 |
| `plugins/autonomous_life.py` | 自主生活循环调度 |
| `core/llm.py` | 主模型与后台模型统一调用层 |
| `core/persona.py` | 人格提示词、关系档案和相关经历检索 |
| `core/world.py` | 世界观读取与约束注入 |
| `core/memory.py` | SQLite 对话、摘要、记忆和心理数据 |
| `core/appraisal.py` | 心理事件候选的评价与程序裁决 |
| `core/life_engine.py` | 自主事件生成、选择和结果结算 |
| `config/` | 可版本管理的人格与世界观设定 |
| `data/` | 本地运行数据；包含隐私，不提交到 Git |
| `tests/` | 离线单元测试 |

## 运行方式

### Windows 一键启动

1. 将本项目与 `NapCat.Shell` 放在同一父目录。
2. 复制 `.env.example` 为 `.env`，填写模型密钥。
3. 确保 NapCat 的反向 WebSocket 地址为 `ws://127.0.0.1:8080/onebot/v11/ws`，并使双方 token 一致。
4. 双击 `一键启动.bat`。

只检查环境、不启动进程：

```powershell
.\一键启动.bat -Check
```

### 手动启动

```bash
python -m venv .venv
.venv/Scripts/python -m pip install -r requirements.txt
.venv/Scripts/python bot.py
```

## 关键配置

```env
# 群聊总开关
GROUP_CHAT_ENABLED=true

# true：仅回复 @；false：回复所有普通群消息
GROUP_AT_ONLY=true

# 自主生活系统
LIFE_ENGINE_ENABLED=true

# 心理评价保持影子模式
APPRAISAL_SHADOW_MODE=true
```

## 数据与隐私

`.env` 包含 API Key 和 OneBot token；`data/` 包含 QQ 用户标识、对话、关系、记忆与角色运行状态；`scheduler_jobs.txt` 包含定时消息接收者的 QQ 号。这些内容均被 `.gitignore` 排除，不应上传到公开或私有远程仓库。定时任务可从 `scheduler_jobs.example.txt` 复制创建。

首次运行会自动创建所需的数据库和状态文件。迁移机器时，如需保留角色连续性，应在停机后单独、加密备份 `data/`。

## 当前质量状态

- Python 源码可通过 `compileall` 编译检查。
- 离线测试覆盖世界观注入、人格与记忆、心理事件验证、自主生活结算、模型传输以及群聊开关。
- 心理成长系统仍处于影子观察阶段；正式写入人格或长期信念前，需要继续验证候选准确率、证据完整性和变化上限。

## 后续建议

1. 给群聊增加群白名单、触发概率和上下文隔离，避免大型群中频繁回复。
2. 增加数据库迁移版本和自动备份机制。
3. 为模型调用增加用量统计、超时指标和更细粒度的失败告警。
4. 持续观察心理事件候选，达到稳定标准后再分阶段退出影子模式。
5. 增加端到端 OneBot 测试，覆盖 NapCat 断线重连和消息发送失败。
